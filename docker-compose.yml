services:
  api:
    build: .
    environment:
      OLLAMA_URL: ${COACH_OLLAMA_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${COACH_OLLAMA_MODEL:-qwen2.5:7b-instruct-q2_K}
    ports:
      - "8000:3000"
